package _360_20190815;

/**
 * 给出两个在m进制下含有n位的数字，你可以分别将这两个数各位上的数字重新排列，
 *然后将n位数按位对应相加并分别对m取模，这样显然可以得到一个新的m进制下的n位数（可能存在前导0），
 * 但是这个结果是不唯一的，那么按照这样的操作，能够得到的最大的m进制下的数字时多少？
 *
 * 输入：
 * 输入第一行为n,m
 * 输入第二行和第三行分别包含n个整数，中间用空格隔开，每个整数都在0到m-1之间。每行第i个数表示的是当前数第i位上的数字。
 *
 * 输出：
 * 输出包含n个数字，中间用空格隔开，表示得到的最大的数字，从高位到低位输出。
 * @author ZOE
 * @date 2019/8/15 20:22
 */
public class Main2 {
    public static void main(String[] args) {

    }

}
